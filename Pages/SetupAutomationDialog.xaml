<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MoscoviumThree.Pages.SetupAutomationDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Width="640" MinHeight="460">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Select items to install/apply:" Margin="0,0,0,12" Style="{ThemeResource BodyStrongTextBlockStyle}" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="AppsPanel" Spacing="8" Padding="0,0,16,0">
                <TextBlock Text="System Tweaks" FontWeight="SemiBold" Margin="0,0,0,0" />
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8" Margin="0,12,0,0">
            <Button Content="Select All Apps" Click="BtnSelectAll_Click" />
            <Button Content="Clear Apps" Click="BtnClearApps_Click" />
            <AppBarSeparator />
            <Button Content="Save Profile..." Click="BtnSaveProfile_Click" />
            <Button Content="Load Profile..." Click="BtnLoadProfile_Click" />
            <TextBlock x:Name="ProfileStatusText" VerticalAlignment="Center" FontSize="12"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

    </Grid>
</UserControl>
