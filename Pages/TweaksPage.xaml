<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MoscoviumThree.Pages.TweaksPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="LogEntryTemplate">
            <Grid ColumnSpacing="10" Padding="0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <FontIcon Glyph="{Binding Glyph}" FontSize="12" VerticalAlignment="Center"
                          Foreground="{Binding GlyphColor}" />
                <TextBlock Grid.Column="1" Text="{Binding Text}" TextWrapping="Wrap" VerticalAlignment="Center" />
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid Padding="36,24,36,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Debloat Tweaks" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,8" />
            <TextBlock Text="Registry tweaks for privacy, a cleaner Explorer and better gaming performance. Runs as administrator."
                       Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" MaxWidth="700" TextWrapping="Wrap" />

            <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,16,0,12">
                <Button x:Name="BtnApplyTweaks" Content="Apply Selected Tweaks" Style="{StaticResource AccentButtonStyle}"
                        Click="BtnApplyTweaks_Click" />
                <Button Content="Select All" Click="BtnSelectAll_Click" />
                <Button Content="Clear" Click="BtnClear_Click" />
            </StackPanel>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="TweaksPanel" Spacing="4" />
        </ScrollViewer>

        <TextBlock Grid.Row="2" Margin="0,12,0,0" FontSize="12"
                   Text="Some tweaks need an Explorer restart or sign out to take effect. Start with 'Create Restore Point' so you can revert anything."
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" MaxWidth="700" TextWrapping="Wrap" />
    </Grid>
</Page>
