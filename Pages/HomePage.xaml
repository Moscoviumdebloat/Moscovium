<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MoscoviumThree.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="36,24,36,24">
        <StackPanel MaxWidth="800" HorizontalAlignment="Left" Spacing="4">

            <!-- Header -->
            <TextBlock Text="Home" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,20" />

            <!-- Debloat -->
            <Button x:Name="BtnDebloat" Style="{StaticResource ActionCardStyle}" Click="BtnDebloat_Click">
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <FontIcon Glyph="&#xE74D;" FontSize="24" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                    <StackPanel Spacing="2">
                        <TextBlock Text="Debloat Windows" Style="{StaticResource CardHeaderStyle}" />
                        <TextBlock Text="Remove bloatware, disable telemetry, and apply privacy tweaks. Optionally install OpenShell, Explorer Patcher, and Nilesoft Shell."
                                   Style="{StaticResource CardDescriptionStyle}" />
                    </StackPanel>
                </StackPanel>
            </Button>

            <!-- Wallpaper -->
            <Button x:Name="BtnWallpaper" Style="{StaticResource ActionCardStyle}" Click="BtnWallpaper_Click">
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <FontIcon Glyph="&#xE91B;" FontSize="24" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                    <StackPanel Spacing="2">
                        <TextBlock Text="Change Wallpaper" Style="{StaticResource CardHeaderStyle}" />
                        <TextBlock Text="Set your desktop wallpaper with support for Fill, Fit, Stretch, Tile, Center, and Span modes."
                                   Style="{StaticResource CardDescriptionStyle}" />
                    </StackPanel>
                </StackPanel>
            </Button>

            <!-- Yabosen CFG -->
            <Button x:Name="BtnYaboCfg" Style="{StaticResource ActionCardStyle}" Click="BtnYaboCfg_Click">
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <FontIcon Glyph="&#xE7FC;" FontSize="24" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                    <StackPanel Spacing="2">
                        <TextBlock Text="Yabosen CS2 Config" Style="{StaticResource CardHeaderStyle}" />
                        <TextBlock Text="Download the latest Yabosen config for CS2 and install it to your game directory automatically."
                                   Style="{StaticResource CardDescriptionStyle}" />
                    </StackPanel>
                </StackPanel>
            </Button>

            <!-- Restart Explorer -->
            <Button x:Name="BtnRestartExplorer" Style="{StaticResource ActionCardStyle}" Click="BtnRestartExplorer_Click">
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <FontIcon Glyph="&#xE72C;" FontSize="24" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                    <StackPanel Spacing="2">
                        <TextBlock Text="Restart Explorer" Style="{StaticResource CardHeaderStyle}" />
                        <TextBlock Text="Kill and restart Windows Explorer. Useful after applying shell customizations."
                                   Style="{StaticResource CardDescriptionStyle}" />
                    </StackPanel>
                </StackPanel>
            </Button>

            <!-- VC++ Runtimes -->
            <Button x:Name="BtnRuntime" Style="{StaticResource ActionCardStyle}" Click="BtnRuntime_Click">
                <StackPanel Orientation="Horizontal" Spacing="16">
                    <FontIcon Glyph="&#xE770;" FontSize="24" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                    <StackPanel Spacing="2">
                        <TextBlock Text="Install Visual C++ Runtimes" Style="{StaticResource CardHeaderStyle}" />
                        <TextBlock Text="Install all Visual C++ Redistributable packages at once."
                                   Style="{StaticResource CardDescriptionStyle}" />
                    </StackPanel>
                </StackPanel>
            </Button>

            <!-- Drag and Drop CFG Zone -->
            <TextBlock Text="CS2 Config Drop Zone" Style="{StaticResource SectionHeaderStyle}" Margin="0,24,0,0" />
            <Border x:Name="DropZone"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource AccentFillColorDefaultBrush}"
                    BorderThickness="2"
                    CornerRadius="12"
                    Padding="24"
                    MinHeight="100"
                    AllowDrop="True"
                    DragOver="DropZone_DragOver"
                    Drop="DropZone_Drop">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8">
                    <FontIcon Glyph="&#xE896;" FontSize="32" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" HorizontalAlignment="Center" />
                    <TextBlock Text="Drop your CS2 .cfg files here"
                               FontSize="16" FontWeight="SemiBold"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="Files will be automatically copied to your CS2 cfg folder"
                               FontSize="12"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               HorizontalAlignment="Center" />
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
